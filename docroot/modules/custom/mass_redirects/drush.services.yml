services:
  mass_redirects.commands:
    class: \Drupal\mass_redirects\Commands\MassRedirectsCommands
    arguments: ['@entity_type.manager', '@database', '@redirect.repository']
    tags:
      - { name: drush.command }
