<?php

use Drupal\Core\StringTranslation\TranslatableMarkup;

function mass_dashboard_local_tasks_alter(&$local_tasks) {
  $local_tasks['system.admin']['title'] = new TranslatableMarkup('Welcome');
  unset($local_tasks['system.admin_index']);
}
