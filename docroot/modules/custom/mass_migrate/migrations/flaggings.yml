id: flaggings
label: Insert Flaggings
source:
  plugin: flaggings
destination:
  plugin: entity:flagging
  default_bundle: watch_content
process:
  entity_type:
    plugin: default_value
    default_value: node
  entity_id: destid1
  uid: uid
migration_dependencies:
  required:
    - service_details
