{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * If we are using the default paragraph, we only want it to print the contactNode.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ contactNode.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{% set level = 3 %}
{% if elements['#level'] is not empty %}
  {% set level = elements['#level'] %}
{% endif %}

{% set addressContactGroup = false %}
{% set phoneContactGroup = false %}
{% set onlineContactGroup = false %}
{% set faxContactGroup = false %}
{% set moreLink = false %}
{% set image = false %}

{% if contactNode.field_ref_address is not empty %}
  {% set addressContactGroup = true %}
{% endif %}
{% if contactNode.field_ref_phone_number is not empty %}
  {% set phoneContactGroup = true %}
{% endif %}
{% if contactNode.field_ref_links is not empty %}
  {% set onlineContactGroup = true %}
{% endif %}
{% if contactNode.field_ref_fax_number is not empty %}
  {% set faxContactGroup = true %}
{% endif %}
ore_info_link is not empty %}
{% if contactNode.field_contact_more_info_link is not empty and current_node_url != (contactNode.field_contact_more_info_link[0].url|render) %}
  {% set moreLink = true %}
{% endif %}
{% if org_logo is not empty %}
  {% set image = true %}
{% endif %}

{% embed "@organisms/contact/contact-row.twig" with {
  "contactRow": {
    "addressContactGroup": addressContactGroup,
    "phoneContactGroup": phoneContactGroup,
    "onlineContactGroup": onlineContactGroup,
    "faxContactGroup": faxContactGroup,
    "moreLink": moreLink,
    "image": image
  }
} %}
  {% block addressContactGroup %}
    {{ contactNode.field_ref_address|view|merge({'#level': level}) }}
  {% endblock %}
  {% block phoneContactGroup %}
    {{ contactNode.field_ref_phone_number|view|merge({'#level': level}) }}
  {% endblock %}
  {% block onlineContactGroup %}
    {{ contactNode.field_ref_links|view|merge({'#level': level}) }}
  {% endblock %}
  {% block faxContactGroup %}
    {{ contactNode.field_ref_fax_number|view|merge({'#level': level}) }}
  {% endblock %}
  {% block moreLink %}
    {% include "@atoms/decorative-link.twig" with {
      "decorativeLink": {
        "href": contactNode.field_contact_more_info_link[0].url,
        "text": contactNode.field_contact_more_info_link[0].title,
        "info": ""
      }
    } %}
  {% endblock %}
  {% block contactImage %}
    {{ org_logo }}
  {% endblock %}
{% endembed %}
