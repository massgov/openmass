{# Alert #}
{% if content.field_alert_text|render is not empty %}
    {{ include('@organisms/by-author/callout-alert.twig', {
        calloutAlert: {
          text: content.field_alert_text,
          href: '',
        },
      }) }}
{% endif %}
