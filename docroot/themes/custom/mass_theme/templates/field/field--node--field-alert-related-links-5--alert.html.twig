{#
/**
 * @file
 * Partial for link list.
 */
#}
{% set relatedLinks = [] %}
{# Use our array to wrap up our links #}
{% for item in items %}
  {% set relatedLinks = relatedLinks|merge([{'href': item.content['#url'], 'text': item.content['#title']}]) %}
{% endfor %}

{%
set linkList = {
  'sidebarHeading': {
    'title': label,
  },
  'links': relatedLinks,
  'stacked': true
}
%}

{% include '@organisms/by-author/link-list.twig' %}
