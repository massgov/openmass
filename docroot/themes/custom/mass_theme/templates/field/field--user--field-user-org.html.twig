{#
/**
 * @file
 * Renders user.field_user_org with its label.
 *
 * Mass theme's global field.html.twig drops labels by design (content
 * pages render values only). The Permission Groups field on user pages
 * needs its label, so this template restores the standard label +
 * items output for that specific field.
 */
#}
{%
  set classes = [
    'field',
    'field--name-' ~ field_name|clean_class,
    'field--type-' ~ field_type|clean_class,
    'field--label-' ~ label_display,
    label_display == 'inline' ? 'clearfix',
  ]
%}
<div{{ attributes.addClass(classes) }}>
  {% if not label_hidden %}
    <h4{{ title_attributes.addClass('label') }}>{{ label }}</h4>
  {% endif %}
  <ul class="field__items">
    {% for item in items %}
      <li{{ item.attributes.addClass('field__item') }}>{{ item.content }}</li>
    {% endfor %}
  </ul>
</div>
