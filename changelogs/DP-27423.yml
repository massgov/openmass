Changes:
  - description: |
      Pins backstop image to a fixed version
      Switches from puppeteer to playwright
      Disables some flaky tests temporarily
      Make the references capture try 3 times
    issue: DP-27423
