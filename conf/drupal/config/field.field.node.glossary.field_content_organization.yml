uuid: 18795ea2-fbd0-4548-87a0-8df2a740c329
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_content_organization
    - node.type.glossary
    - taxonomy.vocabulary.user_organization
id: node.glossary.field_content_organization
field_name: field_content_organization
entity_type: node
bundle: glossary
label: 'Organization Owner Groups'
description: 'The list of Organization Owner Groups that may edit this content. This is setup automatically for new pages based on the group of the page creator. For pages that existed before we added this field, we auto-populated this based on values in the Organization(s) field. This field is visible only to Administrators and Content Administrators currently and is not used for permission yet. After full release, permissions will be enforced, and this field will be visible and changeable for all editors who have permissions.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      user_organization: user_organization
    sort:
      field: _none
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
