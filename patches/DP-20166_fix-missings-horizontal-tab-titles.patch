This patch is a 1.x re-roll of https://www.drupal.org/files/issues/2020-05-14/field_group-3104205-1.patch
index acf08f3..efa4cd3 100644
--- a/formatters/tabs/horizontal-tabs.js
+++ b/formatters/tabs/horizontal-tabs.js
@@ -56,7 +56,10 @@
             summaryElement = $this.find('> summary');
           }

-          var summary = summaryElement.clone().children().remove().end().text();
+          var summary = summaryElement.find('span:first-of-type').text();
+          if (!summary.length) {
+            summary = summaryElement.clone().children().remove().end().text();
+          }
           var horizontal_tab = new Drupal.horizontalTab({
             title: $.trim(summary),
             details: $this
