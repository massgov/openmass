diff --git a/entity_usage_queue_tracking.module b/entity_usage_queue_tracking.module
index 387fbd0e70..7c7445abed 100644
--- a/entity_usage_queue_tracking.module
+++ b/entity_usage_queue_tracking.module
@@ -12,7 +12,6 @@ function entity_usage_queue_tracking_module_implements_alter(array &$implementat
     'entity_update',
     'entity_predelete',
     'entity_translate_delete',
-    'entity_revision_delete',
   ];
 
   if (!in_array($hook, $overrides, TRUE)) {
@@ -80,19 +79,6 @@ function entity_usage_queue_tracking_entity_translation_delete(EntityInterface $
   }
 }
 
-/**
- * Implements hook_entity_revision_delete().
- */
-function entity_usage_queue_tracking_entity_revision_delete(EntityInterface $entity) {
-  if (\Drupal::config('entity_usage_queue_tracking.settings')->get('queue_tracking') && entity_usage_queue_tracking_is_trackable($entity)) {
-    \Drupal::queue('entity_usage_tracker')->createItem([
-      'operation' => 'revision_delete',
-      'entity_type' => $entity->getEntityTypeId(),
-      'entity_id' => $entity->id(),
-    ]);
-  }
-}
-
 function entity_usage_queue_tracking_is_trackable($entity): bool {
   $update_tracker = Drupal::service('entity_usage.entity_update_manager');
   // Get around silly protected method.
diff --git a/src/Plugin/QueueWorker/EntityUsageTracker.php b/src/Plugin/QueueWorker/EntityUsageTracker.php
index 5e9ea8c7cc..d491813008 100644
--- a/src/Plugin/QueueWorker/EntityUsageTracker.php
+++ b/src/Plugin/QueueWorker/EntityUsageTracker.php
@@ -132,6 +132,20 @@ public static function create(ContainerInterface $container, array $configuratio
    */
   public function processItem($data) {
 
+    if (($data['operation'] ?? '') === 'revision_delete') {
+      // Queued revision deletions must never be processed: the queue item does
+      // not carry the deleted revision ID, so the only revision available at
+      // processing time is the current default one — deleting usage records by
+      // that ID destroys usage data of the live revision. Revision deletions
+      // are already handled synchronously by entity_usage itself with the real
+      // deleted revision object.
+      $this->logger->info('Skipped revision_delete queue item for @entity_type:@entity_id.', [
+        '@entity_type' => $data['entity_type'],
+        '@entity_id' => $data['entity_id'],
+      ]);
+      return;
+    }
+
     $this->logger->info('Processing queue item for entity type: @entity_type, entity ID: @entity_id, operation: @operation.', [
       '@entity_type' => $data['entity_type'],
       '@entity_id' => $data['entity_id'],
