diff --git a/js/builder.js b/js/builder.js
--- a/js/builder.js
+++ b/js/builder.js
@@ -732,8 +732,19 @@
         top: scrollPosition,
         behavior: 'auto',
       });
-      triggerElement.focus();
+      // Only refocus if trigger is still in the document (e.g. not removed by delete).
+      const node = triggerElement && (triggerElement[0] || triggerElement);
+      if (node && document.body.contains(node)) {
+        triggerElement.focus();
+      }
     }
   });
 
+  // Prevent any form inside an LPB dialog from full-page submit (delete/edit/add).
+  $(document).on('submit', '.ui-dialog-content form', function (e) {
+    if ($(this).closest('[id^="lpb-dialog-"]').length) {
+      e.preventDefault();
+    }
+  });
+
 })(jQuery, Drupal, Drupal.debounce, dragula, once);
diff --git a/src/Form/DeleteComponentForm.php b/src/Form/DeleteComponentForm.php
--- a/src/Form/DeleteComponentForm.php
+++ b/src/Form/DeleteComponentForm.php
@@ -66,6 +66,8 @@
     $component = $this->layoutParagraphsLayout->getComponentByUuid($this->componentUuid);
     $type = $component->getEntity()->getParagraphType()->label();
     $form['#title'] = $this->t('Delete component', ['@type' => $type]);
+    // Prevent full-page submit from navigating to this route; only AJAX submit should run.
+    $form['#action'] = '#';
     $form['confirm'] = [
       '#markup' => $this->t('Really delete this "@type" component? There is no undo.', ['@type' => $type]),
     ];
@@ -119,12 +121,15 @@
    */
   public function deleteComponent(array $form, FormStateInterface $form_state) {
     $response = new AjaxResponse();
-    $response->addCommand(new CloseDialogCommand(Dialog::dialogSelector($this->layoutParagraphsLayout)));
     if ($this->needsRefresh()) {
-      return $this->refreshLayout($response);
+      $this->refreshLayout($response);
+      $response->addCommand(new CloseDialogCommand(Dialog::dialogSelector($this->layoutParagraphsLayout)));
+      return $response;
     }
+    // Remove from DOM before closing dialog so trigger link is gone and cannot be refocused.
     $response->addCommand(new RemoveCommand('[data-uuid="' . $this->componentUuid . '"]'));
     $response->addCommand(new LayoutParagraphsEventCommand($this->layoutParagraphsLayout, $this->componentUuid, 'component:delete'));
+    $response->addCommand(new CloseDialogCommand(Dialog::dialogSelector($this->layoutParagraphsLayout)));
     return $response;
   }
